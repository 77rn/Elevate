
<%- include('./partials/header') %>
<% if(success.length > 0) { %>
    <div id="successMessage" class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500 transition-opacity duration-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= success %>
        </span>
    </div>
<% } %>

<% if(error.length > 0) { %>
    <div id="errorMessage" class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-red-500 transition-opacity duration-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= error %>
        </span>
    </div>
<% } %>

<script>
    setTimeout(() => {
        const successMessage = document.getElementById('successMessage');
        if (successMessage) {
            successMessage.style.opacity = '0';
            setTimeout(() => successMessage.remove(), 500); 
        }

        const errorMessage = document.getElementById('errorMessage');
        if (errorMessage) {
            errorMessage.style.opacity = '0';
            setTimeout(() => errorMessage.remove(), 500); 
        }
    }, 3000); 
</script>




            <div class="w-full min-h-screen flex items-center justify-center bg-gray-50">
                <div class="w-full max-w-md bg-white p-10 shadow-lg rounded-lg text-center">
                    <div class="mb-8">
                        <h3 class="text-4xl font-light mb-2">Welcome to <span
                                class="text-black font-semibold">Elevate</span></h3>
                        <p class="text-gray-600">Sign in or create an account</p>
                        <button id="toggleButton" class="mt-4 text-sm text-blue-500 hover:underline">
                            Don't have an account? Sign up
                        </button>
                    </div>
                    <form id="loginForm" autocomplete="off" action="/users/login" method="post" class="space-y-4">
                        <div>
                            <div class="relative">
                                <i class="ri-mail-line absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i>
                                <input
                                    class="w-full pl-10 pr-4 py-2.5 border border-gray-200 rounded-lg focus:outline-none focus:border-black transition-colors"
                                    type="email" placeholder="you@example.com" name="email">
                            </div>
                        </div>
                        <div>
                            <div class="relative">
                                <i class="ri-lock-line absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i>
                                <input
                                    class="w-full pl-10 pr-4 py-2.5 border border-gray-200 rounded-lg focus:outline-none focus:border-black transition-colors"
                                    type="password" placeholder="••••••••" name="password">
                            </div>
                        </div>
                        <button type="submit"
                            class="w-full bg-black text-white py-3 rounded-lg hover:bg-gray-800 transition-colors flex items-center justify-center gap-2">
                            Login
                            <i class="ri-arrow-right-line"></i>
                        </button>
                    </form>

                    <form id="registerForm" autocomplete="off" action="/users/register" method="post"
                        class="space-y-4 hidden">
                        <div>

                            <div class="relative">
                                <i class="ri-user-line absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i>
                                <input
                                    class="w-full pl-10 pr-4 py-2.5 border border-gray-200 rounded-lg focus:outline-none focus:border-black transition-colors"
                                    type="text" placeholder="John Doe" name="fullname">
                            </div>
                        </div>
                        <div>
                            <div class="relative">
                                <i class="ri-mail-line absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i>
                                <input
                                    class="w-full pl-10 pr-4 py-2.5 border border-gray-200 rounded-lg focus:outline-none focus:border-black transition-colors"
                                    type="email" placeholder="you@example.com" name="email">
                            </div>
                        </div>
                        <div>
                            <div class="relative">
                                <i class="ri-lock-line absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i>
                                <input
                                    class="w-full pl-10 pr-4 py-2.5 border border-gray-200 rounded-lg focus:outline-none focus:border-black transition-colors"
                                    type="password" placeholder="••••••••" name="password">
                            </div>
                        </div>
                        <button type="submit"
                            class="w-full bg-black text-white py-3 rounded-lg hover:bg-gray-800 transition-colors flex items-center justify-center gap-2">
                            Create Account
                            <i class="ri-arrow-right-line"></i>
                        </button>
                    </form>
                </div>
            </div>

            <script>
                document.getElementById('toggleButton').addEventListener('click', function () {
                    let loginForm = document.getElementById('loginForm');
                    let registerForm = document.getElementById('registerForm');

                    if (loginForm.classList.contains('hidden')) {
                        loginForm.classList.remove('hidden');
                        registerForm.classList.add('hidden');
                        this.textContent = "Don't have an account? Sign up";
                    } else {
                        loginForm.classList.add('hidden');
                        registerForm.classList.remove('hidden');
                        this.textContent = "Already have an account? Login";
                    }
                });
            </script>

            <%- include('./partials/footer') %>